1. Create a secure TTS proxy on the server

Add a new Express route (e.g., server/routes/mrBlueTTS.ts) that accepts POST requests with { text, voiceId }, reads process.env.ELEVENLABS_API_KEY, forwards the request to ElevenLabs, and returns the audio back to the client. A simplified version might look like:

import { Router } from 'express';
import fetch from 'node-fetch';

const router = Router();

router.post('/', async (req, res) => {
  const { text, voiceId } = req.body;
  const apiKey = process.env.ELEVENLABS_API_KEY;
  if (!apiKey) return res.status(500).json({ success: false, error: 'API key missing' });
  try {
    const resp = await fetch(`https://api.elevenlabs.io/v1/text-to-speech/${voiceId}/stream`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'xi-api-key': apiKey
      },
      body: JSON.stringify({ text, model_id: 'eleven_monolingual_v1', voice_settings: { stability: 0.5, similarity_boost: 0.75 } })
    });
    if (!resp.ok) throw new Error(`Status ${resp.status}`);
    const buffer = await resp.arrayBuffer();
    res.set('Content-Type', 'audio/mpeg');
    res.send(Buffer.from(buffer));
  } catch (err) {
    console.error('[tts] proxy error:', err);
    res.status(500).json({ success: false, error: 'TTS proxy failed' });
  }
});

export default router;


Mount this route in your server/app.ts or server/index.ts:

import mrBlueTTS from './routes/mrBlueTTS';
// ...
app.use('/api/mr-blue/tts', mrBlueTTS);


Add ELEVENLABS_API_KEY to your server environment variables (not exposed to the client).

2. Update the client TTS service

Modify client/src/lib/ttsService.ts so that it calls your new proxy instead of ElevenLabs directly:

export async function speak(text: string, voiceId: string): Promise<Blob> {
  const resp = await fetch('/api/mr-blue/tts', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ text, voiceId })
  });
  if (!resp.ok) throw new Error(`TTS proxy error: ${resp.status}`);
  return await resp.blob();
}


Leave the rest of the service unchanged.

3. Supply your GLB models

Place your mr_blue_real.glb and mr_blue_pixar.glb files in public/models/ as described in the README. Without these assets, the avatar will not render.

4. Confirm the backend animation route

The existing server file server/routes/mrBlueAgents.ts already defines POST /avatar/animate. Make sure this router is mounted at the correct base path (usually /api/mr-blue/agents). In your server configuration you should see something like:

import mrBlueAgents from './routes/mrBlueAgents';
app.use('/api/mr-blue/agents', mrBlueAgents);


If it’s missing, add the mount line.

5. Integrate the new 3D avatar page

Follow the instructions from the previous patch to add:

client/src/components/mr-blue/Avatar3D.tsx

client/src/lib/personalityEngine.ts

client/src/hooks/useEmotionDetection.ts

client/src/pages/mr-blue-avatar-3d.tsx

Update App.tsx to import and route the new page.

6. Install dependencies

Ensure @react-three/fiber, @react-three/drei, and three are installed (npm install @react-three/fiber @react-three/drei three).