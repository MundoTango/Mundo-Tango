Build failed
Your deployment attempt had the following errors:
The build process is running out of memory during Vite build. Node.js heap limit reached at ~2GB while transforming 6537 modules.
The application is too large for the available memory during the build phase. The Vite build includes large dependencies like Three.js, Recharts, and many others totaling 3.8MB+ in the main bundle.
The build command 'npm run build' executes both 'vite build' and 'esbuild server/index.ts' which together exceed available memory during the deployment build process.
Agent suggestions

Debug with Agent


Build command: npm run build
(No memory limit set)
Add to deployment secrets/environment:
NODE_OPTIONS=--max-old-space-size=4096

This allocates 4GB heap memory for Node.js during build

"build": "vite build && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist"
"build": "node --max-old-space-size=4096 ./node_modules/vite/bin/vite.js build && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist"

// vite.config.ts - current configuration with no manual chunk splitting
// Add to vite.config.ts build.rollupOptions:
build: {
  rollupOptions: {
    output: {
      manualChunks: {
        'react-vendor': ['react', 'react-dom', 'wouter'],
        'three-vendor': ['three', '@react-three/fiber'],
        'chart-vendor': ['recharts'],
        'ui-vendor': ['@radix-ui/react-dialog', '@radix-ui/react-dropdown-menu']
      }
    }
  }
}

// Direct imports of heavy components
import { VisualEditorPage } from './pages/admin/VisualEditorPage';
import { Avatar3DDesignerPage } from './pages/Avatar3DDesignerPage';
// Lazy load heavy components
const VisualEditorPage = lazy(() => import('./pages/admin/VisualEditorPage'));
const Avatar3DDesignerPage = lazy(() => import('./pages/Avatar3DDesignerPage'));

// vite.config.ts with all optimizations enabled
// Add to vite.config.ts:
build: {
  minify: 'esbuild', // Lighter than terser
  sourcemap: false,  // Disable sourcemaps for production
  chunkSizeWarningLimit: 1000
}